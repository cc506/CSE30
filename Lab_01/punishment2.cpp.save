#include <iostream>
using namespace std;
int main()
{
        int count, typo;
        cout << "Enter the number of lines for the punishment: ";
        cin >> count;
        if(count <= 0){
        cout << "You entered an incorrect value for the number of lines!" << endl;
        }
        else{
	cout <<  "Enter the line for which we want to make a typo: ";
        cin >> typo;
		if(typo < 0){
		cout << "You enterted an incorrect value for the line typo!" << endl;
		}
		else{
		for(int i = 0 ; i< typo; i++){
		if(i == typo)
        	cout << "I will always use objectoriented programing.";
        	}
		}
	for(int i = 0 ; i< count; i++){
                cout << "I will always use objectoriented programming.";
	}
	}
        return 0;
}
